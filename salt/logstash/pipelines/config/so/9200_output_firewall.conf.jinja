output {
  if [dataset] =~ "firewall" {
    elasticsearch {
      hosts => "{{ GLOBALS.manager }}"
      user => "{{ ES_USER }}"
      password => "{{ ES_PASS }}"
      index => "so-firewall"
      ssl => true
      ssl_certificate_verification => false
    }
  }
}
