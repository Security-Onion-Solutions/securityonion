domain: "{{ salt['pillar.get']('sensoroni:analyzers:elasticsearch:domain', '') }}"
authUser: "{{ salt['pillar.get']('sensoroni:analyzers:elasticsearch:authUser', '') }}"
authPWD: "{{ salt['pillar.get']('sensoroni:analyzers:elasticsearch:authPWD', '') }}"
numResults: "{{ salt['pillar.get']('sensoroni:analyzers:elasticsearch:numResults', '') }}"
api_key: "{{ salt['pillar.get']('sensoroni:analyzers:elasticsearch:api_key', '') }}"
index: "_all/"
observable_type: {"hash":"sha256", "blah":"blahblah", }

# timeframe format is YYYY-MM-DD
timeframeStart: "2023-01-01"
timeframeEnd: "2024-01-01"
