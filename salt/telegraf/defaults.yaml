telegraf:
  enabled: False
  config:
    interval: '30s'
    metric_batch_size: 1000
    metric_buffer_limit: 10000
    collection_jitter: '0s'
    flush_interval: '10s'
    flush_jitter: '0s'
    debug: 'false'
    quiet: 'false'
