##! Local site policy.

{%- set ALLOWEDOPTIONS = [ '@load', '@load-sigs', 'redef' ] %}

{%- for k, v in LOCAL.items() | sort %}
  {%- if k|lower in ALLOWEDOPTIONS %}
    {%- for li in v %}
{{ k }} {{ li }}
    {%- endfor %}
  {%- endif %}
{%- endfor %}
