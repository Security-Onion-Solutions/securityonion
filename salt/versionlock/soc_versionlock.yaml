versionlock:
  kernel:
    description: Lock the kernel to prevent upgrade.
    global: True
    forcedType: bool
  hold:
    description: List of packages to hold
    global: True
    forcedType: "[]string"
    multiline: True
